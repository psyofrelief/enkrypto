import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import { faBitcoin } from "@fortawesome/free-brands-svg-icons";

const Loading = () => {
  return <FontAwesomeIcon icon={faBitcoin} className="loading" />;
};

export default Loading;
